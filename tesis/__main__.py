"""CLI Layer utilities for configuring and running the DVWA framework.

This module loads runtime configuration, formats command output, or exposes
entry points for executing the LangGraph workflow from the command line."""
from tesis.cli import main

if __name__ == "__main__":
    raise SystemExit(main())
